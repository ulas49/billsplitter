import { View, Text } from 'react-native'
import React, { useEffect } from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const register = () => {
  
  return (
    <SafeAreaView>
      <Text>registertttt</Text>
    </SafeAreaView>
  )
}

export default register